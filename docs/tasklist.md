# План разработки

## Прогресс

| # | Итерация | Статус | Результат |
|---|----------|--------|-----------|
| 1 | Инфраструктура, конфиг, базовый бот | ✅ Готова | `make run` + ответ на `/start` подтверждены |
| 2 | Интеграция с LLM (без истории) | ✅ Готова | Ответы через LLM без памяти подтверждены |
| 3 | История диалога | ✅ Готова | Контекст диалога сохраняется и используется |
| 4 | Команда /reset | ✅ Готова | Команда очищает контекст текущего чата |
| 5 | Обработка ошибок | ✅ Готова | Fallback и логирование ошибок подтверждены |
| 6 | Docker-запуск | ✅ Готова | Сборка и запуск/остановка через Docker подтверждены |
| 7 | Адаптация роли: инженер мониторинга | ✅ Готова | Промпт и приветствие обновлены, анализ инцидентов подтверждён |
| 8 | Деплой в облако Railway | ✅ Готова | Проект создан, переменные заданы, деплой и запуск в Railway подтверждены |
| 9 | Обновление системного промпта (текст) | ✅ Готова | Новый текстовый промпт и структура ответа подтверждены |
| 10 | Обработка фото аварийных событий | ✅ Готова | Анализ фото без текста подтверждён |
| 11 | Обработка аудио (транскрипция) | ⬜ Не начата | — |

> Статусы: ⬜ Не начата · 🔄 В процессе · ✅ Готова

---

## Итерации

### Итерация 1 — Инфраструктура, конфиг, базовый бот
- [x] Инициализировать проект через `uv` (`pyproject.toml`)
- [x] Добавить зависимости: `aiogram`, `openai`, `python-dotenv`
- [x] Создать структуру папок (`src/`, `tests/`)
- [x] Создать `.env.example` с перечнем всех обязательных переменных
- [x] Создать `Makefile` с командами `install`, `run`, `docker-build`, `run-docker`, `docker-down`
- [x] Реализовать `src/config.py`: загрузка и валидация переменных из `.env`, fail fast при отсутствии обязательных
- [x] Настроить логгирование через stdlib `logging` в `stdout`
- [x] Реализовать `src/main.py` и `src/handlers.py`: запуск polling, обработчик `/start`

**Проверка:** Бот запускается через `make run` и отвечает на `/start` в Telegram.

---

### Итерация 2 — Интеграция с LLM (без истории)
- [x] Реализовать `src/llm.py`: вызов LLM через `openai` client с поддержкой переключения провайдера
- [x] Подключить LLM к обработчику сообщений: каждое новое сообщение отправляется в LLM с системным промптом

**Проверка:** Бот отвечает на любое сообщение через LLM, но не помнит предыдущих сообщений.

---

### Итерация 3 — История диалога
- [x] Реализовать `src/dialog_store.py`: in-memory хранение истории по `chat_id`
- [x] Передавать историю чата в запрос к LLM

**Проверка:** Бот ведёт осмысленный диалог с памятью контекста.

---

### Итерация 4 — Команда /reset
- [x] Добавить обработчик `/reset` в `handlers.py`
- [x] При вызове команды история текущего чата очищается

**Проверка:** После `/reset` бот не помнит предыдущих сообщений.

---

### Итерация 5 — Обработка ошибок
- [x] Обработать ошибки LLM API и сети: fallback-сообщение пользователю
- [x] Логировать причину ошибки на уровне `ERROR`

**Проверка:** При недоступном LLM API бот отвечает корректно, не падает.

---

### Итерация 6 — Docker-запуск
- [x] Написать `Dockerfile`
- [x] Проверить запуск через `make run-docker`

**Проверка:** Бот полностью работает в Docker через `make run-docker`.

---

### Итерация 7 — Адаптация роли: инженер мониторинга
- [x] Обновить `SYSTEM_PROMPT` в `.env.example`: роль инженера мониторинга ИТ-сети с инструкцией по анализу аварий
- [x] Обновить приветственное сообщение `/start` под новую роль

**Проверка:** Бот корректно анализирует описание аварийного события и предлагает вероятные причины и шаги локализации.

---

### Итерация 8 — Деплой в облако Railway
- [x] Убедиться, что `Dockerfile` совместим с Railway (точка входа, порт не нужен для polling)
- [x] Создать проект в Railway, подключить GitHub-репозиторий
- [x] Задать переменные окружения в Railway (все из `.env.example`)
- [x] Выполнить деплой и убедиться, что бот запущен в облаке

**Проверка:** Бот работает в Telegram из облака Railway без локального запуска.

---

### Итерация 9 — Обновление системного промпта (текст)
- [x] Обновить текстовый системный промпт под роль узкоспециализированного старшего инженера мониторинга ИТ-сети
- [x] Зафиксировать в промпте структуру ответа: классификация, влияние, вероятные причины, шаги устранения
- [x] Добавить обязательный блок сравнения: самый быстрый и самый эффективный способ устранения
- [x] Обновить приветственное сообщение и/или инструкцию по формату входного текста для пользователя

**Проверка:** В текстовом режиме бот стабильно выдаёт структурированный разбор аварии в новой роли.

---

### Итерация 10 — Обработка фото аварийных событий
- [x] Добавить обработчик входящих изображений в Telegram
- [x] Реализовать отдельный клиент анализа изображений (по умолчанию OpenRouter)
- [x] Добавить отдельный промпт и модель для сценария анализа фото
- [x] Подключить единый формат ответа с рекомендациями по устранению

**Проверка:** Пользователь отправляет фото аварийного события/оборудования, бот выполняет анализ и выдает рекомендации.

---

### Итерация 11 — Обработка аудио (транскрипция)
- [ ] Добавить обработчик голосовых сообщений в Telegram
- [ ] Реализовать отдельный аудио-клиент с нативной поддержкой OpenRouter (`input_audio` base64), без Whisper
- [ ] Добавить отдельный промпт и модель для аудио-сценария
- [ ] Встроить результат транскрипции/интерпретации в общий сценарий анализа аварии

**Проверка:** Пользователь отправляет голосовое сообщение, бот корректно обрабатывает его и выдает структурированные рекомендации.
